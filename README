Simple example of ExtJS 4 MVC application.
It's a CRUD for managing users accounts written in Ruby On Rail.

How to run in:

* bundle install --path=vendor/bundle
* rake db:migrate
* rake db:seed
* rails s
* And finally navigate to http://localhost:3000
